module semantics/semantics

imports
  src-gen/ds-signatures/Graceless-sig
  semantics/runtime/environment
  semantics/runtime/store
  semantics/interpret
  semantics/values

// Entry point

signature
  arrows
    Term -init-> Value

rules

  term : Term -init-> value
    where Env {}, Self Init() |- term :: Store {} --> value.
